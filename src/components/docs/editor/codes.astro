---
export const codet = `class Player extends Fragment{
    constructor(){
    super();
    this.attatchComponent("hitbox")
    }
}`;
export const codeb = `this.hitBoxes.addHitBox({
      name: "closeContact",
      offset: { x: -20, y: -20, w: 80, h: 40 },
      active: true,
    });`;
export const active = `  this.hitboxes.activateHitBox(hitbox, value)`;
---
